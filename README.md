# 3DAI Engine - Terminal 3D Simulator

Deterministic C++ pipeline for generating Gaussian-weighted 3D networks and exporting to R for interactive visualization.

## Build

```bash
make clean && make all
